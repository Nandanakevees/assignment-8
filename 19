original_list = ['red', 'black', 'white', 'green', 'orange']
substring_to_search = 'ack'

filtered_list = list(filter(lambda s: substring_to_search in s, original_list))

print("Elements containing the substring:", filtered_list)
